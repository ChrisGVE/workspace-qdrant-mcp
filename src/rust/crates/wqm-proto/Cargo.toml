# wqm-proto -- N24 gRPC contract: the proto SoT + generated code + the `Address`
# enum (ARCH §9.1). Generated code only, no wqm-crate dependencies, so it stays
# linkable by every surface. P04-GT001 declares the crate; the .proto services
# and tonic/prost codegen arrive with N24's slice, P04-GT011. (The prior comment
# credited these to "N13 proto definitions" -- N13 is observability; N24 owns the
# proto SoT, ARCH rev15 §9.1.)
[package]
name = "wqm-proto"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
