# Universal across all language fixtures.
# Language-specific build artifacts live in each language's own .gitignore
# (e.g. fortran/.gitignore owns *.mod, so go/go.mod is never matched).

# Editors / IDEs
.idea/
.vscode/

# Validation run output
results/
