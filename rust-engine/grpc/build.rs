// Build script for gRPC proto generation

fn main() -> Result<(), Box<dyn std::error::Error>> {
    // For now, just a placeholder build script
    // In the future, this will generate Rust code from proto files

    // tonic_build::configure()
    //     .build_server(true)
    //     .build_client(true)
    //     .compile(&["../proto/ingestion.proto"], &["../proto"])?;

    Ok(())
}
